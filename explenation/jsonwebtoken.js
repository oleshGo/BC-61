// const jsonwebtoken = require("jsonwebtoken");

// const payload = {
//   id: "slkjghdf",
// };

// const foo = async () => {
//   const token = await jsonwebtoken.sign(payload, "qwerrt", { expiresIn: "1s" })

//   const data = await jsonwebtoken.verify(token, "qwerrt");
//   console.log(data);
// };

// foo();
